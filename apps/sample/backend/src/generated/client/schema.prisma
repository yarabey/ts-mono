generator client {
  output   = "../src/generated/client"
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model GreetingTemplate {
  locale   String @id
  template String
}
