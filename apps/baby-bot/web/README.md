# baby-bot-web — unpopulated stub

Intentionally **not implemented**. The `adopt-baby-ai-as-baby-bot` change
populated only `backend-bot` and `mini-app`; this scaffold is left as a stub
(out of scope per the proposal's Non-Goals). Do not build features here without
a new change/ADR.
